[package]
name = "protocol-ble"
version = "0.1.0"
edition = "2024"

[dependencies]
uuid = {path="../../submodules/uuid",optional = true}
trouble-host = {path="../../submodules/trouble/host",optional = true}

[features]
uuid = ["dep:uuid"]
trouble-host = ["dep:trouble-host"]