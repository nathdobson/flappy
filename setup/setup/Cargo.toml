[package]
name = "setup"
version = "0.1.0"
edition = "2024"

[dependencies]
proto = {path = "../../common/proto"}
nusb = {version="0.2.1",features = ["tokio"]}
tokio = { version = "1.48.0", features = ["rt", "rt-multi-thread", "macros","io-util"] }